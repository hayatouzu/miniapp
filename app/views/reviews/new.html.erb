<div class="row">
    <div class="row">
          <%= form_for @review, url: {action: 'create'}, do |f| %>
            <div style="margin: 8px 0">
              <div>
                <textarea cols="30" name="text" placeholder="text" rows="10"><%= @review.text %></textarea>
                <%# <%= f.text_field :text, class: "hogehoge" %> 
              </div>
            </div>
          <div class="row">
            <div class="col10 push1">
              <%= f.submit class: "btn btn--block" do %>
              投稿する<i class="icon-arrow-right"></i>
              <% end%>
            </div>
          </div>
        <% end %>
    </div>
</div>